# Spectrum — how it appears.
# This theme owns presentation only. Content types, taxonomy, and URLs
# belong to the Pulse repo that consumes this theme.

[params]
  # Overridden by the consuming site.
  tagline = ""

[params.spectrum]
  # The one knob that changes the whole palette.
  accentHue = 355
